{
	"buildCommand": "npm install && npm run build:vercel",
	"installCommand": "npm install",
	"framework": "sveltekit",
	"version": 2,
	"routes": [{ "src": "/(.*)", "dest": "/" }],
	"env": {
		"DISABLE_TURTLE_DB": "true",
		"NODE_VERSION": "18.x"
	}
}
